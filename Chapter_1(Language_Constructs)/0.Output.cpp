#include<iostream>
using namespace std; // the input and output commands are included inside this namespace

int main()
{
    cout<<"Hello";  // std::cout<<"hello" *If std namespace is not added*
    return 0;
}